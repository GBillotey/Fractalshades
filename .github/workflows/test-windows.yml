# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: test-windows

on:
#   push:
#    branches: [ master ]
#  pull_request:
#    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:

    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [3.8] #, 3.9, 3.10]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

#    - name: Set up MinGW
#      uses: egor-tensin/setup-mingw@v2
#      with:
#        platform: x64
    
    - name: Install gmpy2 and adds the headers
      run: |
        dir
        python -m pip install --upgrade pip
        # Download and install from https://github.com/msys2
        python -m pip install https://files.pythonhosted.org/packages/8d/6f/b87d79fe5b764b97eb5599c5a8ef351fec3b688201d4706c6146e4601d1d/gmpy2-2.1.2-cp38-cp38-win_amd64.whl
<<<<<<< HEAD
        # Patch gmpy2 install
        python win_build/test_gmp2.py
        # echo $GITHUB_PATH
        # echo "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin" >> $GITHUB_PATH
        # echo "*** after ** "
        # echo $GITHUB_PATH
=======
        # Test gmpy2 install
        dir
        python test_gmp2.py
        
        C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin\x86_64-w64-mingw32-gcc.exe --version

        echo "$GITHUB_PATH"
        echo "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin" >> $GITHUB_PATH
        echo "*** after ** "
        echo "$GITHUB_PATH"
        
        x86_64-w64-mingw32-gcc.exe --version
>>>>>>> a97b4fd019b439a338d55ffc562703f4eadeafb8

    - name: Install dependencies
      run: |
        echo "install build dependencies"
        python -m pip install -r win_build/built_requirements.txt
        echo "install libraries needed for the test"
        python -m pip install flake8 pytest
        python -m pip install -r win_build/build_requirements.txt
        dir

    - name: Builds Fractalshades with no build isolation
      run: |
        python3 -m build
        dir
        # python -m pip install --no-deps --no-build-isolation git+git://github.com/${{ github.repository }}.git@${{ github.sha }}

    - name: Builds Fractalshades with no build isolation
      run: |
<<<<<<< HEAD
        python -m pip install -r requirements.txt
        
=======
        echo "install fractalshades"
        python -m pip install --no-deps --no-build-isolation git+git://github.com/${{ github.repository }}.git@${{ github.sha }}
>>>>>>> a97b4fd019b439a338d55ffc562703f4eadeafb8

    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Test with pytest
      run: |
        python -m pytest

